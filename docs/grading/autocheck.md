# Auto-check System

The checker reads `submission.json`, validates JSON, and checks blockchain evidence using RPC/explorer APIs. It can verify Ethereum transactions/events, Ethernaut completions, Polkadot extrinsics, and TON transactions.
