# Deadline-gated Checks

Use `workflow_dispatch` for manual final checks and `schedule` for deadline checks. Python checker can switch between practice and final grading mode by comparing current UTC time to deadline.
